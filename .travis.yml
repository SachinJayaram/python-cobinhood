language: python

python:
  - '2.7'

install:
  - pip install codecov
  - pip install -r requirements.txt

script:
  - python -m unittest cobinhood.tests.test_cobinhood
  - py.test cobinhood/tests/*.py --cov=codecov

after_success:
  - codecov
